<!-- Header partial-->
    <%- include ('./partials/head.ejs') %>
    <link rel="stylesheet" href="/styles/home.css">
<body>
    <h1>My Bookworm</h1>
    <ul>
        <li><a href="/books/newbook">Add a New Book</a></li>
        <li><a href="/members/newmember">Add a New Member</a></li>
    </ul>
    <div class="flex-container">
        <h1 class="members">Members</h1>
        <ul>
            <% for (let member of members) { %>
                <a href="/members/<%= member._id %>">
                    <li class="member-object">
                        <h2 class="member_names">
                            <%=member.name%>
                        </h2>
                    </li>
                </a>
            <% } %>
    </div>

    <div class="flex-grid">
                <% for (let book of books) { %>
                    <a href="/books/<%= book._id %>">
                        <figure class="book-card">
                            <img alt="<%= book.name %>" src="<%= book.img%>" width="300">
                            <figcaption>
                                <h5>
                                    <%=book.name%>
                                </h5>
                                <h6>
                                    <%= book.description %>
                                </h6>
                            </figcaption>
                        </figure>
                    </a>
                    <% } %>
    </div>

<!-- Footer Partial -->
    <%- include ('./partials/footer.ejs') %>
</body>
